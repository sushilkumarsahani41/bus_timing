import 'package:flutter/material.dart';

class ToCollege extends StatelessWidget {
  const ToCollege({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("To College"),
      ),
    );
  }
}
